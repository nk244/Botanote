# Botanote 開発フロー

Issue対応の対応計画作成からPR発行までの一連の流れをSkillで進める手順です。

---

## 全体の流れ

```
/feature-design <N>  →  /design-review <N>  →  /implement <N>  →  /test <N>  →  /pr-review <PR番号>  →  /release
```

---

## ステップ1: 機能設計書の作成

```
/feature-design <Issue番号>
```

- GitHubのIssueを取得してコードベースを調査し、設計書を `.claude/designs/issue-<N>.md` に作成する
- 完了後、次のステップが案内される

---

## ステップ2: 設計書のレビュー

```
/design-review <Issue番号>
```

- 作成した設計書をアーキテクチャ規約・コーディング規約・DB安全性などの観点でレビューする
- **承認**された場合: `status: approved` に更新され、次のステップが案内される
- **修正依頼**の場合: 指摘事項を確認して設計書を修正し、再度 `/design-review <N>` を実行する

---

## ステップ3: 実装 & PR作成

```
/implement <Issue番号>
```

- `main` を最新化してからブランチを作成し、設計書に従って実装する
- `flutter analyze` → `flutter test` を実施し、問題がなければコミットする
- PR作成前に `main` の最新をブランチに取り込んでからプッシュ・PR発行する
- **プッシュ前にユーザーへ確認を求める**

---

## ステップ4: UI/UXレビュー

```
/ux-review <Issue番号>
```

- 実装した画面をアプリ利用者視点で確認し、文言・操作フロー・一貫性などの問題を報告する
- 🔴 要修正の項目はその場で修正してコミットする
- Androidエミュレーター（`Medium_Phone_API_36.1`）で実際の画面を撮影して視覚的に確認する
- PRマージ前に必ず実施する

---

## ステップ5: テスト確認観点の確認（任意）

```
/test <Issue番号>
```

- 静的解析・テスト実行結果と手動確認チェックリストをレポートする
- PRのマージ前に実機動作を確認する際の観点として活用する

---

## ステップ6: PRレビュー & マージ

```
/pr-review <PR番号>
```

- PRの差分をレビューし、`flutter analyze` / `flutter test` / CI・コンフリクト状態を確認する
- 全てのマージ前チェックを通過した場合のみ、AIがマージまで実施する（CLAUDE.md「マージはAIが行わない」の例外）
- 🔴 必須修正がある場合はマージせず変更依頼し、修正後に `/pr-review <N>` を再実行する

---

## PR マージ後

1. Issueのクローズを確認する
2. `/release` を実行し、リリースビルド（APK）を作成してGoogle Driveにアップロードする

---

## ステップ7: リリース

```
/release
```

- リリースビルド（APK）を作成し、Google Driveにアップロードする
- PRがマージされ、`main` に変更が反映された後に実行する
- ユーザーから明示的に「今回はリリース不要」の指示がない限り、PRマージ後の一連の流れとして実施する

---

## コマンド早見表

| コマンド | 内容 |
|---|---|
| `/feature-design <N>` | Issue調査・設計書作成 |
| `/design-review <N>` | 設計書レビュー・承認 |
| `/implement <N>` | 実装・コミット・PR発行 |
| `/ux-review <N>` | UI/UX・文言をユーザー視点でレビュー・修正 |
| `/test <N>` | 静的解析・テスト・手動確認観点レポート |
| `/pr-review <PR番号>` | PR差分のレビュー・マージ前チェック・マージ |
| `/release` | リリースビルド（APK）作成・Google Driveアップロード |
